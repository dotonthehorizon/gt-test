============
Installation
============

At the command line::

    $ pip install GTrends-Test

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv GTrends-Test
    $ pip install GTrends-Test
